role: feature-section
twig_name: FeatureSection
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:FeatureSection headline="{{ lorem_title() }}" subheadline="{{ lorem_sentence() }}" />
