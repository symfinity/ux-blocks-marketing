role: faq
twig_name: Faq
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:Faq headline="{{ lorem_title() }}" />
