role: integrations-section
twig_name: IntegrationsSection
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:IntegrationsSection headline="{{ lorem_title() }}" />
