role: content-section
twig_name: ContentSection
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:ContentSection headline="{{ lorem_title() }}" body="{{ lorem_paragraph() }}" />
