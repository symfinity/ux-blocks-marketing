role: hero
twig_name: Hero
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:Hero headline="{{ lorem_title() }}" subheadline="{{ lorem_sentence() }}" primaryActionLabel="{{ lorem_words(1)|capitalize }}" primaryActionHref="#" />
  - id: motion
    section: Motion
    layout: stack
    items:
      - id: motion
        label: "Fade up"
        props: {}
        slot_twig: |
          <twig:Hero headline="{{ lorem_title() }}" subheadline="{{ lorem_sentence() }}" primaryActionLabel="{{ lorem_words(1)|capitalize }}" primaryActionHref="#" />
