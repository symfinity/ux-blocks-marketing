role: cookie-consent
twig_name: CookieConsent
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:CookieConsent headline="{{ lorem_title() }}" description="{{ lorem_sentence() }}" />
