role: cta-band
twig_name: CtaBand
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:CtaBand headline="{{ lorem_title() }}" subheadline="{{ lorem_sentence() }}" primaryActionLabel="{{ lorem_words(1)|capitalize }}" primaryActionHref="#" />
