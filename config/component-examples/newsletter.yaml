role: newsletter
twig_name: Newsletter
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:Newsletter headline="{{ lorem_title() }}" description="{{ lorem_sentence() }}" />
