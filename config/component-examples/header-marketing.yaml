role: header-marketing
twig_name: HeaderMarketing
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:HeaderMarketing headline="{{ lorem_title() }}" />
