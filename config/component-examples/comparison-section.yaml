role: comparison-section
twig_name: ComparisonSection
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:ComparisonSection headline="{{ lorem_title() }}" />
