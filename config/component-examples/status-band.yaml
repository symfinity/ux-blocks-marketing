role: status-band
twig_name: StatusBand
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Operational
        props: {}
        slot_twig: |
          <twig:StatusBand headline="{{ lorem_sentence() }}" statusTone="operational" />
