role: team
twig_name: Team
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:Team headline="{{ lorem_title() }}" />
