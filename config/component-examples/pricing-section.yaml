role: pricing-section
twig_name: PricingSection
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: "Two tiers"
        props: {}
        slot_twig: |
          <twig:PricingSection headline="{{ lorem_title() }}" subheadline="{{ lorem_sentence() }}" />
