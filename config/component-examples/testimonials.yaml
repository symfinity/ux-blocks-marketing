role: testimonials
twig_name: Testimonials
groups:
  - id: variants
    section: Variants
    layout: stack
    items:
      - id: default
        label: Default
        props: {}
        slot_twig: |
          <twig:Testimonials headline="{{ lorem_title() }}" />
