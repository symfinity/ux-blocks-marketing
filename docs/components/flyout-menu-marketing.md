# FlyoutMenuMarketing

Role `flyout-menu-marketing` · fragment `blocks.marketing.flyout-menu-marketing`

**Extended dependency (025):** When `enhanced=true` and `symfinity/ux-blocks-extended` is installed, delegates to `DropdownMenu` (`blocks.ext.dropdown-menu`). Otherwise nat `<ul>` fallback — no marketing Stimulus.
