# ComparisonSection

Role `comparison-section` · fragment `blocks.marketing.comparison-section`

REF: Tailark / Launch UI comparison matrices.

Props: `headline`, `subheadline`, `columns[]` (`label`, `highlighted`), `rows[]` (`feature`, `cells[]` with `value`, `emphasized`).
