# CookieConsent

Role `cookie-consent` · fragment `blocks.marketing.cookie-consent`

REF: Open Policy consent layout. Sibling to V0 `banner` — not a promotional strip.

Props: `headline`, `description`, `categories[]`, `acceptLabel`, `rejectLabel`, `customizeLabel`, `policyLinks[]`.

Host wires persistence; package renders static consent UI only.
